# TAI-EvalGenTCS CLI Requirements
# Python 3.11+

# OpenAI client (compatible with OpenRouter)
openai>=1.12.0

# Environment variables management
python-dotenv>=1.0.0

# JSON schema validation (optional, for development)
jsonschema>=4.20.0

# Testing (optional, for development)
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
